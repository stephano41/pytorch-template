defaults:
  - pipeline: pt_tune
  - hydra/run/dir: job_timestamp
  - _self_
  - optional local_env: env
  - override hydra/job_logging: custom

folder: ???

status: test
output_root: ./outputs
seed: 122
name: null
moniotr: f1_macro
mode: max