checkpoint_dir: ???
checkpoint_name: ???

metrics:
  - _target_: srcs.metrics.accuracy
  - _target_: srcs.metrics.top_k_acc

defaults:
  - data_loader: mnist_test
  - override hydra/job_logging: custom

  - _self_

hydra:
  run:
    dir: ${checkpoint_dir}/../../test-${now:%Y-%m-%d}-${now:%H-%M-%S}
