checkpoint: ???

metrics:
  - _target_: srcs.metrics.accuracy
  - _target_: srcs.metrics.top_k_acc

defaults:
  - _self_
  - data: mnist_test

  - override hydra/job_logging: custom

hydra:
  run:
    dir: no_chdir
